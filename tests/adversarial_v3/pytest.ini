[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    adversarial: marks tests as part of the adversarial v3 suite
    tier1: external scanner tests (requires SB_ADV=1)
    tier2: external vendor tests (triple opt-in)
    tier3: real browser tests (requires --backend non-stub)
addopts = -v --tb=short
